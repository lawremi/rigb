# -*- org -*-
* TODO Get FileTypeHandler from BundleContext
  #+BEGIN_SRC java
    ServiceReference igbServiceReference = bundleContext.getServiceReference(FileTypeHandler.class.getName());
    
    if (igbServiceReference != null)
        {
            FileTypeHandler igbService = (FileTypeHandler) bundleContext.getService(igbServiceReference);
            registerServices(igbService);
        }
    
  #+END_SRC

  #+BEGIN_SRC java
    
    
    BundleContext bundleContext = OSGiHandler.getInstance().getBundleContext();
    
    ServiceReference[] fileTypeHandlerReferences =
        bundleContext.getServiceReferences(FileTypeHandler.class.getName(), null);
    
    for (ServiceReference fileTypeHandlerReference : fileTypeHandlerReferences) {
        FileTypeHandler fileTypeHandler = (FileTypeHandler)
            bundleContext.getService(fileTypeHandlerReference);
        System.out.println("@@@@@@@@@ " +
                           fileTypeHandler.getName() + ", " +
                           fileTypeHandler.getExtensions()[0] +
                           "(objectClass=" + rIGBHandler.class.getName() + ")");
    }
    
  #+END_SRC
* TODO Swing from R.
  #+BEGIN_SRC java :tangle FrameDemo.java
    import java.lang.Thread;
    import java.awt.*;
    import java.awt.event.*;
    import javax.swing.*;
    
    /* FrameDemo.java requires no other files. */
    public class FrameDemo {
        /**
         * Create the GUI and show it.  For thread safety,
         * this method should be invoked from the
         * event-dispatching thread.
         */
        public static Thread thread;
    
        private static void createAndShowGUI() {
            //Create and set up the window.
            JFrame frame = new JFrame("FrameDemo");
            frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    
            JLabel emptyLabel = new JLabel("");
            emptyLabel.setPreferredSize(new Dimension(175, 100));
            frame.getContentPane().add(emptyLabel, BorderLayout.CENTER);
    
            //Display the window.
            frame.pack();
            frame.setVisible(true);
        }
    
        public static void main(String[] args) {
            // Schedule a job for the event-dispatching thread:
            // creating and showing this application's GUI.
            FrameDemo.thread = new Thread(new Runnable() {
                    public void run() {
                        createAndShowGUI();
                    }
                });
            try {
                javax.swing.SwingUtilities.invokeAndWait(FrameDemo.thread);
            } catch (Throwable e) {
                e.printStackTrace();
            }
        }
    }
    
  #+END_SRC
* TODO Control IGB from R.
  This works:

  #+BEGIN_SRC java :tangle igb-from-java.bsh :shebang #!/usr/bin/env bsh
    addClassPath("inst/lib/igb_exe.jar");
    // addClassPath("inst/lib/igb.jar");
    // addClassPath("inst/lib/felix.jar");
    addClassPath("inst/lib/genometry.jar");
    // addClassPath("inst/lib/genoviz.jar");
    // addClassPath("inst/lib/igb_exe.jar");
    // addClassPath("inst/lib/igb.jar");
    addClassPath("inst/lib/igb_service.jar");
    // addClassPath("inst/lib/tribble-0.1.jar");
    // addClassPath("inst/lib/affx_fusion.bnd.jar");
    // addClassPath("inst/lib/BigWig.jar");
    // addClassPath("inst/lib/Bookmark.jar");
    // addClassPath("inst/lib/colt.bnd.jar");
    // addClassPath("inst/lib/common.jar");
    // addClassPath("inst/lib/commons-codec-1.4.jar");
    // addClassPath("inst/lib/commons-io-2.0.1.jar");
    // addClassPath("inst/lib/commons-lang3-3.0.jar");
    // addClassPath("inst/lib/commons-net-2.0.bnd.jar");
    // addClassPath("inst/lib/ExternalView.jar");
    // addClassPath("inst/lib/felix.jar");
    // addClassPath("inst/lib/freehep-2.1.1.bnd.jar");
    // addClassPath("inst/lib/genometry.jar");
    // addClassPath("inst/lib/genoviz.jar");
    // addClassPath("inst/lib/GraphAdjuster.jar");
    // addClassPath("inst/lib/igb_exe.jar");
    // addClassPath("inst/lib/igb.jar");
    // addClassPath("inst/lib/igb_service.jar");
    // addClassPath("inst/lib/image4j.bnd.jar");
    // addClassPath("inst/lib/jlfgr-1_0.bnd.jar");
    // addClassPath("inst/lib/jython.bnd.jar");
    // addClassPath("inst/lib/log4j-1.2.11.bnd.jar");
    // addClassPath("inst/lib/picard.bnd.jar");
    // addClassPath("inst/lib/plugins_page.jar");
    // addClassPath("inst/lib/PropertyView.jar");
    // addClassPath("inst/lib/REngine.jar");
    // addClassPath("inst/lib/RestrictionSites.jar");
    // addClassPath("inst/lib/sam.bnd.jar");
    // addClassPath("inst/lib/SearchModeIdOrProps.jar");
    // addClassPath("inst/lib/SearchModeResidue.jar");
    // addClassPath("inst/lib/SearchView.jar");
    // addClassPath("inst/lib/swing-layout.bnd.jar");
    // addClassPath("inst/lib/tribble-0.1.bnd.jar");
    // addClassPath("inst/lib/tribble-0.1.jar");
    // addClassPath("inst/lib/window_service_def.jar");
    // addClassPath("inst/lib/window_service.jar");
    // addClassPath("jar:file:///home/pcdanenb/prg/java/rigb/inst/lib/igb_exe.jar!/igb.jar");
    addClassPath("dist/rIGB.jar");
    // import *;
    // print("singleton: " + com.affymetrix.igb.IGB.getSingleton().toString());
    com.affymetrix.main.Main.main(new String[]{});
    // handler = com.affymetrix.main.OSGiHandler.getInstance();
    // action = com.affymetrix.igb.shared.OpenURIAction();
    // java.lang.Thread.sleep(10000);
    handler = com.affymetrix.main.OSGiHandler.getInstance();
    handler.uninstallBundle("rIGB");
    handler.installBundle("file:///home/pcdanenb/prg/java/rigb/dist/rIGB.jar");
    context = handler.getInstance().getBundleContext();
    references = context.getServiceReferences(com.affymetrix.genometryImpl.parsers.FileTypeHandler.class.getName(), null);
    references = context.getServiceReferences(com.affymetrix.genometryImpl.parsers.FileTypeHandler.class.getName(), "(name=rIGB)");
    // references = context.getServiceReferences(com.affymetrix.genometryImpl.parsers.FileTypeHandler.class.getName(), "(objectClass=" + com.gene.bcb.rigb.rIGBHandler.class.getName() + ")");
    // references = context.getServiceReferences(com.affymetrix.genometryImpl.parsers.FileTypeHandler.class.getName(),
    //                                           "(objectClass=" + com.affymetrix.genometryImpl.parsers.FileTypeHandler.class.getName() + ")");
    // references = context.getServiceReferences(com.affymetrix.genometryImpl.parsers.FileTypeHandler.class.getName(),
    //                                           "(objectClass=" + com.gene.bcb.rigb.rIGBHandler.class.getName() + ")");
    print("#################################");
    print(context);
    // reference = context.getServiceReferences(com.affymetrix.igb.osgi.service.IGBService.class.getName(), null);
    // reference = context.getServiceReference(com.affymetrix.genometryImpl.parsers.FileTypeHandler.class.getName());
    // reference = context.getServiceReference(com.affymetrix.genometryImpl.operator.transform.FloatTransformer.class.getName());
    // reference = context.getAllServiceReferences(com.affymetrix.genometryImpl.parsers.FileTypeHandler.class.getName(), null);
    // reference = context.getAllServiceReferences(com.affymetrix.genometryImpl.operator.transform.FloatTransformer.class.getName(), null);
    // references = context.getServiceReference(com.affymetrix.genometryImpl.parsers.FileTypeHandler.class.getName());
    print("context: " + context);
    print("references: " + references);
    for (reference: references) {
        // May need to filter handler here (better way to do it: get text
        // filter to work supra).
        handler = context.getService(reference);
        print(handler.i);
        print("reference: " + reference);
        print("handler: " + handler);
    }
    // print(references.toString() + context.toString() + handler.toString() + "@@@@@@@@@@@@@@@@@@");
    
  #+END_SRC

  But a naive translation into rJava doesn't:

  #+BEGIN_SRC R :tangle igb-from-R.R :shebang #!/usr/local/bin/R --vanilla --slave -f
    library(rJava)
    .jinit(classpath='inst/lib/igb_exe.jar')
    J("com.affymetrix.main.Main")$main(.jarray(c(), contents.class="[S"))
    ## utils <- J("com.affymetrix.common.CommonUtils")$getInstance()
    ## utils$uninstallBundle("rIGB")
    ## utils$installBundle("/home/pcdanenb/prg/java/rigb/dist/rIGB.jar")
    
    ## Can't prevent R from exiting in non-interactive mode; even with the
    ## following:
    ## 
    ## .jinit(classpath='.')
    ## J("FrameDemo")$main(.jnull())
    ## J("FrameDemo")$thread$join()
    
  #+END_SRC
* DONE URI-encoding
  CLOSED: [2011-09-19 Mon 21:18]
  From [[http://www.faqs.org/rfcs/rfc3986.html][RFC3986]]:

  #+BEGIN_QUOTE
  When authority is present, the path must either be empty or begin
  with a slash ("/") character.  When authority is not present, the
  path cannot begin with two slash characters ("//").
  #+END_QUOTE

  #+BEGIN_SRC java :tangle encode-uri.bsh :shebang #!/usr/bin/env bsh
    import java.net.URI;
    import java.io.File;
    
    addClassPath("inst/lib/commons-io-2.0.1.jar");
    import org.apache.commons.io.FilenameUtils;
    
    public static String idToUri(String id) {
        return new URI("igb", null, "///" + id + ".rigb", null).toString();
    }
    
    public static String uriToId(String uri) {
        return FilenameUtils.removeExtension(new File(new URI(uri).getPath()).getName());
    }
    
    id = "harro thar";
    uri = idToUri("harro thar");
    
    print(uri.equals("igb:///harro%20thar.rigb"));
    print(id.equals(uriToId(uri)));
    
  #+END_SRC
* Publication-vectors
  - Statistical graphics and computational statistics newsletter
    (rjavax stuff more interesting)
  - Bioconductor conference
  - Note in Bioinformatics (one or two page articles an Applications
    section: IGB)
  - JSS:
  - Plugins more technical; R and IGB talking: Bioinformatics.
  - R-Journal: Rjavax (peer-reviewed);
  - Nucleic acids research (app note)
  - Genome research (app note)
  - Interface conference might take over for the DSC.
  - RDynload
  - RIGB nice application
